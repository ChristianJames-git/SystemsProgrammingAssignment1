#include <iostream>

#include "Opcode.h"
int main(int argc, char **argv) {
    if (argc != 2) {
        cout << "Please input correct number of files";
        exit(EXIT_FAILURE);
    }

    return 0;
}
